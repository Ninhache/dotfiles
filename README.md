

```bash
git clone https://github.com/Ninhache/dotfiles.git ~/dotfiles
~/dotfiles/bootstrap.sh
```

--

```bash
scripts/gpu-mode.sh intel|nvidia        # GPU de rendu de Hyprland
scripts/sys-sync.sh diff|pull|install   # dépôt <-> /etc
```

